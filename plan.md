1. Add Csound Score export support (`crates/orpheus-lang/src/csound_export.rs`).
   - Done. Added `csound_export::export_number_pattern_to_csound`.
2. Expose the new module in `crates/orpheus-lang/src/lib.rs`.
   - Done.
3. Complete pre commit steps to ensure proper testing, verification, review, and reflection are done.
4. Submit the change with branch name, title and description representing Nova's idea.
