1. **Apply missing pre-commit instructions**: I will call `pre_commit_instructions` tool and verify that all pre-commit steps are successfully executed.
2. **Commit the changes**: Using the `submit` tool, I will commit and push the changes under a descriptive branch name `havoc-f64-parsing-limits` with a clear commit message.
