## 2026-03-24 - [The "Missing Link": Missing Examples in Export and Stats Functions]
**Confusion:** Export formatting functions (e.g., `export_sample_pattern_to_svg`, `export_sample_pattern_to_html`, `export_number_pattern_to_txt`) and analysis tools (`sample_pattern_stats`) in `orpheus-lang` had no `/// ## Examples` executable blocks. This made it difficult for developers to understand how to correctly extract a pattern from an evaluation environment and interact with these APIs.
**Clarification:** Added executable doctests to all functions in `crates/orpheus-lang/src/svg.rs`, `html.rs`, `txt.rs`, and `stats.rs` that utilize `eval_module` to parse a string, extract the pattern with `.get("...").unwrap().as_sample_pattern().unwrap()`, and pass it to the export functions, demonstrating correct instantiation and usage.

## 2026-03-24 - [The "Missing Link": Missing Examples for Number Pattern Exporters and AST Nodes]
**Confusion:** Similar to sample pattern exporters, the `export_number_pattern_to_*` functions (CSV, JSON, MD) in `crates/orpheus-lang/src/export.rs` lacked executable `## Examples`. Furthermore, the foundational parser types `Module` and `Expr` in `crates/orpheus-lang/src/ast.rs` were completely missing examples and documentation explaining their purpose in the Phase 1 parser. Finally, `crates/orpheus-dsp/src/routing.rs` lacked documentation explaining the roles of its internal graph state structures (`TrackState`, `BusState`, `RoutingSnapshot`, etc.).
**Clarification:** Added executable doctests to the `export_number_pattern_to_*` functions using `eval_module("x = 1 2 3")`. Added high-level explanatory documentation and parsing examples to `Module` and `Expr`. Added descriptive documentation to the internal structures in `routing.rs` (examples were omitted for these structs because they reside in a private module and cannot be instantiated directly in doctests).
