Let's look at `crates/orpheus-lang/src/tui.rs`:
```rust
    fn render(self, state: &str, transport: &TransportView) -> Line<'static> {
        let mut spans = Vec::new();
        if let Some(legend) = self.legend {
...
```
`spans` could be `Vec::with_capacity(7)` or something.

Let's look at `crates/orpheus-lang/src/parser.rs`:
```rust
    let mut statements = Vec::new();
    for (start_line, chunk) in chunks {
...
```

```rust
fn split_top_level_bindings(source: &str) -> Vec<(usize, String)> {
    let mut bindings = Vec::new();
```

Let's look at `crates/orpheus-lang/src/eval.rs`:
```rust
fn eval_stack(
        &self,
        layers: &[Expr],
        meter: Option<&MeterContext>,
    ) -> Result<Value, EvalError> {
        let mut values = Vec::with_capacity(layers.len());
```

```rust
    fn eval_call_with_args(
        &self,
        call_expr: &Expr,
        callee: &Expr,
        args: &[Expr],
        piped_args: Vec<Value>,
        meter: Option<&MeterContext>,
    ) -> Result<Value, EvalError> {
...
```
Passing `Vec::new()` to `eval_call_with_args` does not cause multiple reallocations, it just allocates once.

Let's look at `crates/orpheus-pattern/src/stream.rs`:
```rust
    pub fn try_query(&self, span: &TimeSpan) -> Result<Vec<Event<T>>, PatternError>
    where
        T: Clone,
    {
        if span.is_empty() {
            return Ok(Vec::new());
        }

        let mut events = Vec::new();
        for event in &self.events {
```
This is a really good target. `stream.rs` is a core runtime abstraction evaluated constantly in Orpheus. By changing `Vec::new()` to `Vec::with_capacity(self.events.len())`, we eliminate reallocation of the result vector when explicit event streams are queried.

I'll outline this as the performance improvement. It fulfills the criteria: measurable impact (fewer allocs on hot path), <50 lines, safe, idiomatic.
