Let me refine the plan for Vantage:
To create an issue, wait, I can just create the Markdown file in `docs/plans/osc_interoperability.md` or submit the markdown content in the `submit` description.
Ah, the `Vantage` persona instructions from memory:
"Write product specs (presented as a PR or Issue titled '🔭 Vantage: Spec for [Feature Name]') containing: a 'User Story' ('As a [User]...'), the 'So What?' (Business Problem), Metric Definition (Success criteria), Gap Analysis, Acceptance Criteria, and Out of Scope items."

So the steps:
1. Create `docs/plans/osc_interoperability.md` with the required sections.
2. Complete pre-commit steps.
3. Call `submit` with title "🔭 Vantage: Spec for OSC Interoperability", putting the spec in the `description` or just a brief description and pushing the file.

Wait, is there any feature request mentioned in the prompt that I missed?
"Your mission is to ensure we are building *useful* software, not just *complex* software. You define the "What" and the "Why."
There is no specific feature requested! The prompt just says "You are Vantage... Your mission... VANTAGE'S DAILY PROCESS: ANALYZE... DEFINE... PRIORITIZE... PRESENT."
This means I have to look at the codebase, identify a vague idea, and define a spec for it.

Let's look at `docs/design/orpheus_design.md` again.
"10. Open Questions
1. MIDI output? (Already done via midi_output_support.md)
2. Live coding performance model. (performance_mode_keybindings.md)
3. Effect routing. (richer_effects_chain.md handles effects, but not routing)
4. Persistence of REPL state. (repl_history_persistence.md)
5. Collaboration / sharing. Beyond git (since songs are code), is there value in a package manager for sharing samples, synth definitions, or pattern libraries?
6. Integration with existing tools. OSC support for talking to SuperCollider, Ableton Link for tempo sync with other apps/musicians?
7. Visual feedback for patterns. (tracker_visualization.md)"

The vague ideas in Open Questions:
- "Collaboration / sharing. Beyond git, is there value in a package manager for sharing samples, synth definitions, or pattern libraries?" -> Package Manager (e.g. `opm` Orpheus Package Manager).
- "Effect routing. How are effects applied — per-pattern, per-layer, or via a global effects bus?" -> Global Effects Bus.
- "Integration with existing tools. OSC support for talking to SuperCollider..." -> OSC Interoperability.

Let's choose **OSC Interoperability**. It aligns perfectly with the live coding focus.

I will write the spec for "OSC Interoperability".

Let's do `request_plan_review`.
