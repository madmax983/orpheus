--- eval.rs
+++ eval.rs
@@ -1074,13 +1074,19 @@
                 let capacity = events
                     .len()
                     .checked_mul(multiplier)
                     .ok_or_else(|| EvalError::new("section pattern capacity overflowed"))?;
+                if capacity > 100_000 {
+                    return Err(EvalError::new("evaluation exceeded the maximum allowed event limit"));
+                }
                 Ok(Self::Sample(Vec::with_capacity(capacity)))
             }
             Self::Number(events) => {
                 let capacity = events
                     .len()
                     .checked_mul(multiplier)
                     .ok_or_else(|| EvalError::new("section pattern capacity overflowed"))?;
+                if capacity > 100_000 {
+                    return Err(EvalError::new("evaluation exceeded the maximum allowed event limit"));
+                }
                 Ok(Self::Number(Vec::with_capacity(capacity)))
             }
