--- crates/orpheus-pattern/src/rational.rs
+++ crates/orpheus-pattern/src/rational.rs
@@ -452,10 +452,12 @@
     fn rational_converts_to_f64() {
         let r = Rational::new(3, 4).unwrap();
         let f: f64 = r.into();
+        #[allow(clippy::float_cmp)]
         assert_eq!(f, 0.75);

         let ref_r = &Rational::new(1, 2).unwrap();
         let f2: f64 = ref_r.into();
+        #[allow(clippy::float_cmp)]
         assert_eq!(f2, 0.5);
     }
 }
--- crates/orpheus-dsp/src/sample_manifest.rs
+++ crates/orpheus-dsp/src/sample_manifest.rs
@@ -51,8 +51,7 @@
     fn source(&self) -> Option<&(dyn core::error::Error + 'static)> {
         match self {
-            Self::Io { .. } => None,
-            Self::Parse { .. } => None,
+            _ => None,
         }
     }
 }
